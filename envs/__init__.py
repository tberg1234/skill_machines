import envs.moving_targets, envs.office_world, envs.safety_gym

# import pkgutil

# for loader, module_name, is_pkg in pkgutil.walk_packages(__path__):
#     if "." not in module_name: loader.find_module(module_name).load_module(module_name)
